#include <iostream>
using namespace std;
int main()
{
   int i;
   for(i=0;==100;i++)
       
       if ( (i % 5 != 0 ) && ( i % 3 == 0))
       {
           std::cout << " numero divisible por 3 py no por 5:"<<i << std::endl;
       }
   return 0;
}